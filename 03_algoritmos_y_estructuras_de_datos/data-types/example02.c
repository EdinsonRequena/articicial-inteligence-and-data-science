#include <stdio.h>
#include <limits.h>

int main(){   
    printf("char = %d\n", sizeof(char));
    printf("int = %d\n", sizeof(int));

    printf("Min int = %d\n", INT_MIN);
    printf("Max int = %d\n", INT_MAX);

    return 0;
}